<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{url_for('main.home')}}">Marvel Heroes</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
      {% if not current_user.is_authenticated %}
        <a class="nav-link" href="{{url_for('auth.register')}}">Create an Account</a>
        <a class="nav-link" href="{{url_for('auth.signin')}}">Sign in</a>
      {% else %}
        <a class="nav-link" href="{{url_for('auth.logout')}}">Logout</a>
        <a class="nav-link" href="{{url_for('social.collection')}}">Collection</a>
        <a class="nav-link" href="{{url_for('social.user_page', username=current_user.username)}}">My Page</a>
      {% endif %}
      </form>
      </div>
    </div>
  </div>
</nav>